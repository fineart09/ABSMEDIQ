<?xml version="1.0" encoding="UTF-8"?>
<configuration>
  <system.webServer>
    <!-- Minimal config: rely on IIS defaults; no duplicate defaultDocument entries -->
  </system.webServer>
  <!-- If URL Rewrite is installed and you need SPA fallback, we can add rules later. -->
</configuration>
